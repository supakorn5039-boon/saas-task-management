info:
  name: Delete User (admin)
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/admin/users/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

docs: |
  Admin-only. Soft-deletes the user (sets deleted_at). Self-delete is rejected.

settings:
  encodeUrl: true
