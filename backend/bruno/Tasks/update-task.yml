info:
  name: Update Task
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/tasks/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "completed": true
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
