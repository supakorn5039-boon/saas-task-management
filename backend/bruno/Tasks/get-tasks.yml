info:
  name: Get Tasks
  type: http
  seq: 1

http:
  method: GET
  url: "{{dev}}/tasks"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
