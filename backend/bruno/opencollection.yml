opencollection: 1.0.0

info:
  name: SaaS Task Management API

request:
  auth:
    type: bearer
    token: "{{token}}"

bundled: false

extensions:
  bruno:
    ignore:
      - node_modules
      - .git
