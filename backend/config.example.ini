[server]
port       = 8080
production = false
; jwt_secret must be at least 32 characters. Generate one with:
;   openssl rand -hex 32
; You can also set JWT_SECRET in the environment to override this value.
jwt_secret = replace-me-with-a-long-random-string-at-least-32-chars

[database]
host     = localhost
port     = 5432
user     = username
password = password
database = postgres
